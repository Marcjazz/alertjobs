<style>
  #jobs-filter {
    padding: 5px;
    display: grid;
    text-align: center;
  }

  #jobs-filter>div {
    width: 80%;
    justify-self: center;
  }

  #select-filter>div>input {
    box-shadow: 1px 1px 16px var(--background-color);
    background-color: white;
    box-sizing: border-box;
    border-radius: 15px;
    height: 35px;
    padding: 9px;
  }

  #areas {
    margin-top: 20px;
    display: grid;
    grid-auto-flow: column;
    justify-content: center;
    justify-self: start;
  }

  #preloader {
    display: grid;
    grid-auto-flow: column;
    justify-content: center;
    min-height: 50vh;
    align-items: center;
  }
</style>
<script crossorigin src="https://unpkg.com/react@18/umd/react.development.js"></script>
<script crossorigin src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"></script>
<div id="jobs-filter" style="padding: 5px; text-align: center;">
  <h4 style="font-weight: bold;">Retrouvez les jobs de votre localité</h4>
  <div id="select-filter" style="display: flex; gap: 10px;justify-content: center;">
    <select name="location" style="width: 80%;">
      {{#each countries}}
      <optgroup label="{{country_name}}">
        {{#each locations}}
        <option value="{{location_id}}">{{town}}</option>
        {{/each}}
      </optgroup>
      {{/each}}
    </select>
    <select name="tag" style="width: 80%;">
      {{#each tags}}
      <option value="CDD">{{tag_name}}</option>
      {{/each}}
    </select>
  </div>
</div>
<div id="jobs-root">
  <div id="preloader">
    <h4 style="font-weight: bold;">ALERT JOBS</h4>
    <div class="progress">
      <div class="indeterminate"></div>
    </div>
  </div>
</div>
<script src="/js/react.js"></script>